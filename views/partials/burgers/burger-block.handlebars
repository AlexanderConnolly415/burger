<li>
	{{name}}

	<button class="change-hungry" data-id="{{id}}" data-newsleep="{{hungry}}">
		{{#if hungry}}EATING TIME!{{else}}OPEN UP!{{/if}}
	</button>

	<button class="delete-burger" data-id="{{this.id}}">DELETE!</button>
</li>
